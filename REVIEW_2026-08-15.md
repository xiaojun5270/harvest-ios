# iOS 原生端检查报告（2026-08-15）

审查范围：`ios-native/` 全部 8 个 Swift 文件（26,093 行）、`Harvest.xcodeproj`、`Info.plist`、
`Assets.xcassets`、CI workflow，并对照 Flutter 侧 `lib/` 做功能一致性核查。

**审查方式与限制**：当前环境无 Swift 工具链，**没有真正编译过**。所有结论都是静态审查 +
代码路径分析，行号已在当前文件版本上复核（审查期间 `DownloadFeatures.swift` 被编辑过，
行号以 ±20 行内定位为准，请以代码摘要为准）。"是否编译通过""实际卡顿程度"必须在 Xcode 里验证。

先说结论：**这份代码的基础质量比预期好** —— 全项目 0 处 `try!`、0 处 `as!`、0 处 `fatalError`、
0 处 TODO/FIXME、0 处死注释代码；TLS 无任何绕过；WebView 注入值走 `JSONSerialization` 转义正确；
图表全用 Swift Charts 而非手写 Path。原生端也不是空壳：SSE 真流式、WebSocket 真双通道、
兑换是完整 JS 注入循环、接口覆盖面与 Flutter 打平（Flutter 活跃 86 条 vs 原生使用 86 条，**无一条 Flutter 在用而原生打不通**）。

问题集中在三条系统性主线，加上一批"写了但 UI 摸不到"的死代码。

---

## 一、必须先修的 10 条

| # | 问题 | 位置 | 影响 |
|---|---|---|---|
| 1 | `double()` 放行 NaN/Inf，下游 `Int()` 裸转 | `Core.swift:2227-2233` → `PrimaryFeatures:3073`、`DownloadFeatures:2850`、`SiteFeatures:6291` | 后端/网页返回 `"nan"` 即进程崩溃 |
| 2 | 真机/Archive 必然签名失败 | `project.pbxproj:216, 241` `DEVELOPMENT_TEAM = ""` | 唯一**确定性**的构建失败项 |
| 3 | Release 缺 `SWIFT_OPTIMIZATION_LEVEL` | `project.pbxproj:189-207`（Debug 有 `-Onone`，Release 未设→取默认 `-Onone`）+ `SWIFT_COMPILATION_MODE = singlefile` | 发布包是未优化二进制 |
| 4 | 等级 `days` 单位错，**7 倍误差** | 原生 `SiteFeatures:4056-4059, 4082` 当"天"；Flutter `site_level_sheet.dart:838, 939` 当"周"；原生生成器自己标"注册周数要求"（`SiteFeatures:2330`）自相矛盾 | 所有等级进度条显示偏乐观 7 倍 |
| 5 | `Dictionary(uniqueKeysWithValues:)` 重复 key = `preconditionFailure` | `DownloadFeatures.swift:858, 910`、`SecondaryFeatures.swift:3114` | 与 #6 的不稳定 id 叠加即可复现崩溃 |
| 6 | `abs(hashValue)` 当兜底 id | `DownloadFeatures:44`、`SiteFeatures:74`、`SecondaryFeatures:23` | String hash 每次启动换种子 → 重启后 id 变化，sheet/缓存错乱；碰撞会误删、发脏 ID |
| 7 | 缺 `PrivacyInfo.xcprivacy` | 全仓无此文件；命中 FileTimestamp(`Core.swift:551-563`) + UserDefaults(49 处) | App Store 上传 ITMS-91053 直接退回（README 明说要签成现有 app 的下一版） |
| 8 | WebView auth hook 的目标主机来自**网页可控的 localStorage** | `ManagementFeatures.swift:4659-4683` | 站点一个 XSS 即可把 Bearer token 外发到任意域，且不校验 `https` |
| 9 | 并发 401 各自刷新令牌 | `Core.swift:1713-1723, 2008-2029`，`refreshAccessToken()` 无 in-flight 合并 | 后端开轮转+黑名单时用户被踢下线 |
| 10 | 聚合搜索的筛选/排序/历史三块 UI 全是死代码 | `ResourceFilterSheet`（`SecondaryFeatures:1813`）无 present 点；`selectResourceSort`（:1335）零调用方；历史持久化了但从不渲染 | 相比 Flutter 是明显功能退化 |

---

## 二、三条系统性主线

**A. 没有统一的数值清洗层。** `Core.swift` 的 `double()` 对 `"nan"`/`"inf"`/`"1e400"` 全部解析成功
（Swift `Double(_:)` 的行为），下游几十个消费点裸接：`Int(value)` 陷阱崩溃（上表 #1）、
`formatBytes` 的 `Int64(1e30)` 同样崩（`Core.swift:2276-2281`，只挡了 `isFinite` 没挡量级）、
`ProgressView(value:total:)` 的 `max(NaN, 1)` **返回 NaN**（`PrimaryFeatures:2257/2277/2947/2952`，
因为 `max` 实现是 `y >= x ? y : x`）。放大器是 `SiteFeatures:3971-3974` 把 TOML 里的 `"9999PB"` 乘成 1.12e19。
同一个表达式在 `DownloadFeatures:3389` 写对了（有 clamp）而 `335` 漏了，说明是遗漏不是设计。
**在 `double()` 里加一行 `guard number.isFinite` 就能覆盖大半下游。**

**B. 没有稳定的实体身份。** `abs(hashValue)` 兜底 id（上表 #6）+ 17 处 `id: \.offset`
（SecondaryFeatures 7 / PrimaryFeatures 5 / ManagementFeatures 2 / SiteFeatures 2 / DownloadFeatures 1）。
其中 `SecondaryFeatures.swift:1719` 后果最重：`displayedResources` 是 SSE 增量流经 filter+sort 的结果，
**每来一行整个顺序都变**，用下标做身份 = 每次追加全量重绘 + 滚动跳动 + 行内动画错位；
同文件 `:1542` 已有现成的 `resourceIdentifier(item)` 可直接用。

**C. 主线程承担全部数据加工。** `DownloadFeatures:598-605` 8 个同构 `didSet` + `:781-789` 的
`resetFilters()` 连写 7 个属性 → 一次点击触发 7-9 次全量 filter+sort（排序谓词里还有字典查找和
`localizedCaseInsensitiveCompare`，比 `<` 慢约两个数量级），`.searchable` 每敲一个字符走一遍，
并会产出 `Publishing changes from within view updates` 紫色警告。WebSocket 每帧在主线程做全表哈希
（每条种子 20 次 `hasher.combine`）+ 每个 tracker 一次 `URL(string:)` 解析 + 线性查站点标签
（`DownloadFeatures:1569-1625`，`Task.detached` 只挪走了 JSON 解码，最贵的部分没挪）。
`ImageRenderer` 同步渲染约 24M 像素的分享长图（`PrimaryFeatures:1524-1536`，`scale=3` 时近 100MB，
主线程冻结数秒，nil 分支还静默无提示）。`SiteFeatures:2072-2073, 2125-2128` 的 TOML 编辑器
每个 `TextField` binding getter 每帧重解析全文，40 个字段 = 40+ 次，中文输入明显卡顿。

---

## 三、安全与隐私

已确认的 🔴 项，按利用难度排序：

- **auth hook 主机可被网页污染**（上表 #8）——最严重，能直接外发后端 token。
- **导航策略无条件 `.allow`**（`ManagementFeatures:4423-4434, 4456-4469）：除 `.torrent`/magnet 拦截外
  全放行，`window.open`/第三方广告 iframe 可把带站点 Cookie 和 Bearer hook 的主框架导到任意 URL。
  顺带 `mailto:`/`tel:` 因不转交 `UIApplication.open` 而静默失败。
- **WebView 用持久 `.default()` 数据存储且 `logout()` 不清理**（`ManagementFeatures:4340`、`Core.swift:2031-2042`）：
  换账号后上个账号的站点登录态仍在；`Library/WebKit` 未设 `isExcludedFromBackup`，随 iCloud 备份带出。
  完整清理只在"清空全部数据"入口里有。
- **注入 Cookie 缺 HttpOnly**（`ManagementFeatures:4560-4575`），叠加上面两条被放大。
  正面确认：`.domain` 用精确 host、不设 `.expires`，**不存在 A 站 Cookie 写到 B 站的问题**。
- **凭据全文一键进系统剪贴板**，全库 `localOnly`/`expirationDate` 零命中
  （`SiteFeatures:5708-5714`、`ManagementFeatures:2474`、`DownloadFeatures:3055-3056` 磁力链含 passkey）
  → 任意前台 App 可读，并同步到同 iCloud 的 Mac/iPad。
- **应用内更新走 20 个第三方 GitHub 镜像且下载包零校验**（`ManagementFeatures:1088-1092, 896-905`），
  测速本身把用户 IP 暴露给 20 个未审计站点，scheme 白名单还含 `http`。
- **ATS 全关**（`Info.plist:29` `NSAllowsArbitraryLoads`），且未单独声明
  `NSAllowsArbitraryLoadsInWebContent`，WKWebView 直接继承全局放行。

README 第 88-90 行"凭据不写入持久快照"的承诺**部分不成立**：`Core.swift:355-364` 的脱敏黑名单
只有 password/passwd/secret/token 是 `contains`，`cookie/passkey/authkey/apikey/localstorage` 是**等值匹配**，
所以 `rss_key`→`rsskey`、`site_cookie`→`sitecookie`、`user_authkey` 全部漏过；
`sanitizedCachedURL` 只剔 query 不处理路径，而私有 Tracker 的 passkey 恰在路径里，
`DownloadFeatures:1127` 确实把 announce 写进缓存。仪表盘原始响应整包落盘，含各站点 username/email 明文
（`PrimaryFeatures:450-458`）。README 第 22-23 行"signed image URLs remain memory-only"在字节层面也不成立
（`SharedViews.swift:128-130` 的 `storePersistentData` 没有 `guard persistToDisk`）。

`Info.plist:32` 声明了 `NSFaceIDUsageDescription` 但全库零 `LocalAuthentication` 调用 ——
要么落实 Face ID 门禁，要么删掉这个键（未使用的用途串会触发审核问询）。

**后台能力全缺**：无 `UIBackgroundModes`、无 `URLSessionConfiguration.background`、无 `BGTaskScheduler`。
结果是更新包下载切后台即挂（1 小时 resource timeout 形同虚设），公告轮询只在 `scenePhase == .active` 跑、
`trigger: nil` 立即投递 —— 所谓"本地通知"实际只在 App 活跃时出现，杀死状态收不到。
建议要么上远程推送，要么在 README 里标注为前台限定。

---

## 四、与 Flutter 端的功能差距

接口层无缺口，差距在交互层。除上表 #4（days 单位）和 #10（搜索三件套死代码）外，按重要度：

1. **TOML 结构化编辑器只能改已存在的行**（`SiteFeatures:2269-2275` 按行号替换 +
   `guard lines.indices.contains`），无"添加字段"。模板里没写 `sign_info_*`/`torrent_*_rule`/`page_mybonus`
   时这些分组根本不渲染也无法补写。Flutter 有 `ensureField(key)` 补齐全套（`site_config_generator_dialog.dart:2252`）。
   README 第 60 行"structured site-configuration generation"描述过于笃定。
2. **等级里程碑用数组下标而非 `level_id`**（`SiteFeatures:3993` vs Flutter `site_level_milestone.dart` 比 `levelId <= currentId`）
   —— TOML 里等级顺序与 `level_id` 不一致时判错当前等级。
3. **任务中心不是一级 Tab**：`SharedViews.swift:1303-1312` 只有 tag 0/1/2/3/5，**没有 tag 4**；
   Flutter 是底栏一级入口（`shell_page.dart:63, 70`）。能力完整（种子迁移已核实为真实现），是可达性退化。
4. **搜索失败弹全局模态**（`SecondaryFeatures:1211` 每个失败站点都 `presentedError`），
   Flutter 只追加一行红色日志 —— 多站点失败时会连弹。另外逐站点进度日志在首批结果落地后不可见（:1693-1699）。
5. **站点加载失败会不可逆删掉用户勾选**：`SecondaryFeatures:1288-1291` 做
   `selectedSiteIDs.intersection(available)` 后 `persistDraft` 写回 `UserDefaults`，站点临时不可用时配置永久丢失。
6. **推种降级**：分类/标签从真实列表 chip 降级为自由文本；payload 少 `tid`（Flutter `push_torrent_sheet.dart:1633-1644` 同发 `tid` 和 `ids`）；无预览卡。
7. **兑换机制不同**：原生直接 `fetch(target, options)`（`SiteFeatures:5325`），Flutter 是点击页面真实按钮的
   多策略级联（`browser_page.dart:5745-5838`）—— 依赖按钮 handler 做 CSRF/referrer 的站点原生会失败。
   原生还主动过滤掉赠送/捐赠类兑换项（`SiteFeatures:5247` 的正则），Flutter 明确包含。
8. 其余：签到历史硬上限 12 条无展开（`SiteFeatures:4300, 4317`）、仪表盘 treemap 降级为列表且行数硬编码 10、
   镜像地址/站点标签/生成器枚举字段全降级为自由文本、TMDB/豆瓣 backdrop 大图缺失、轮播角标缺失、
   桌面级 16 态状态筛选无原生对应、Transmission 备用限速星期选择器缺失、搜索结果分类名未本地化。

**`MIGRATION_AUDIT.md` 需要修正的表述**：第 40 行 "torrent creation" 两端都没有（把"推送种子"写成了 qB 种子制作器）；
第 253 行 "virtual Tracker filtering" 找不到实据；第 277-279 行 "underlying workflows remain available"
对桌面 16 态筛选不成立；第 100 行 "expandable history records" 与 `prefix(12)` 矛盾；
第 13 行 86 条 `APIPath` 实为 93 声明 / 86 引用（7 个死常量）。

**存疑待运行期确认**：后端配置项 key 名两端不一致 —— `corp_secret` vs `corpsecret`、
`today_say` vs `todaysay`、`chat_id`/`token` vs `telegram_chat_id`/`telegram_token`
（Flutter `option_page.dart:151, 360, 309-310` vs 原生 `ManagementFeatures:2224, 2284, 2268-2269`）。
若这些是线协议 key，微信/TG 配置会**静默保存失败**；若只是 Flutter 本地 controller key 则无事。
建议优先核实这一条。

---

## 五、编译与工程

**Xcode 26 打开时不要点 "Update to recommended settings"。** 当前 `objectVersion=56`、
`LastUpgradeCheck=1600`、`SWIFT_VERSION=5.0`、未设 `SWIFT_STRICT_CONCURRENCY`（即 minimal）。
一旦被带到 Swift 6 语言模式，现在沉默的东西会全部变硬错误：`Core.swift:714` 的
`static let shared` 非 Sendable 全局可变状态、`Core.swift:1694` 的 `api()` 返回 `Any` 跨 MainActor
边界（全项目最普遍的模式）、6 处 `@unchecked Sendable`、`ManagementFeatures:888` 的闭包
（目前侥幸没炸只因为闭包体第一句就是 `DispatchQueue.main.async`，任何人把赋值上移一行就是无声数据竞争）。
想迁移的话顺序是：`[String: Any]` 换 `enum JSONValue: Sendable` → `APIClient` 改 actor
（`AppLogStore`/`AppSessionCache` 已经是 actor，方向对）→ 开 `SWIFT_STRICT_CONCURRENCY = targeted`。

**类型检查超时风险**（无法编译所以只能给风险清单）：24 个 `body`/`some View` 超过 95 行，
最长 `PrimaryFeatures.swift:1166-1399`（**234 行**，`dashboardModule(_:)` 单 switch 内联全部视图树）、
`SiteFeatures.swift:4486-4684`（199）、`5381-5572`（192）；22 个单行超过 230 字符，
最长 `SecondaryFeatures.swift:2110`（**524 字符**）。这些是
`unable to type-check this expression in reasonable time` 的典型来源。叠加 `SiteFeatures.swift`
单文件 6671 行 + Release 的 `singlefile` 模式，增量编译近似全文件重编译。
最有效的缓解是给中间变量标显式类型（`let color: Color = ...`）并把 switch 每个 case 抽成独立 `struct`。

**内存泄漏**：`ManagementFeatures:1071` 带 delegate 的 `URLSession` 只在完成回调里
`finishTasksAndInvalidate()`，`AppUpdateViewModel` 无 `deinit` → VM 提前释放则 session 永久驻留还在耗流量。
`DownloadFeatures:1563-1565` 的 watch task `[weak self]` 只在进入方法前弱持，进入后整个循环强持 self，
退出条件 `isWatching` 只能由外部改 → `self → watchTasks[id] → task → self` 闭环，`onDisappear` 没走到就永久存活。

**静默 catch 11 处**，其中 `DownloadFeatures:1624` 最要命：`catch { }` 吞掉 WebSocket 地址无效/401/TLS 失败，
紧接着无条件 3 秒重连 → 无日志无提示无退避的死循环，用户只看到速度永远是 0。
`SecondaryFeatures:701/1293/2708/2724` 把失败伪装成空结果（`catch { sites = [] }`），
UI 显示"没有可搜索的站点"而不是"加载失败"。项目已有 `recordAppLog`/`AppLogStore` 通道，这些地方没用。

**工程配置已逐项核对通过**（这部分没问题）：8 个 swift 文件全在 Sources 阶段、Assets 在 Resources、
`GENERATE_INFOPLIST_FILE=NO` + `INFOPLIST_FILE` 配置正确无冲突、scheme **已 shared**（CI 的 `-scheme Harvest` 可用）、
`CURRENT_PROJECT_VERSION=291` 与 README 一致、bundle id `com.ptools.harvest`、
`IPHONEOS_DEPLOYMENT_TARGET=17.0` 且实测**没有裸用 iOS 18/26 独占 API**（3 处 `#available` 保护齐全，
实际下限由 `ContentUnavailableView`/双参数 `onChange`/`.contentMargins` 决定 = 17.0，与 README 声称一致）、
AppIcon 1024×1024 RGB 无 alpha 符合上架要求、无需 entitlements（无远程推送/App Groups/Keychain 共享组）、
CI 的 `ditto -c -k --keepParent Payload` 生成的 IPA 结构正确、URLSession 超时有分层配置、
Keychain 用 `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly` 选得对。

**CI 小问题**：`softprops/action-gh-release@v2` 用可变 tag 未 pin SHA + workflow 级 `contents: write`
（tag 被劫持即可发布任意 Release 资产）；未 pin `DEVELOPER_DIR`，`macos-26` 镜像滚动升级会改变工具链；
`timeout-minutes: 30` 对 26k 行单模块冷编译偏紧；dSYM 未上传，sideload 版崩溃无法符号化。
另外深色模式：`LaunchBackground.colorset` 的 dark 变体也是纯白 + `HarvestApp.swift:149` 硬编码
`Color.white` → 深色模式启动白闪。

---

## 六、建议的修复顺序

第一批（几行改动，收益最大）：`double()` 加 `isFinite` 一行 → `uniqueKeysWithValues` 全换
`uniquingKeysWith:` → `refreshAccessToken` 加 in-flight 合并 → `DownloadFeatures:1624` 加日志和指数退避 →
`project.pbxproj` 填 Team ID + Release 加 `-O`/`wholemodule`。

第二批（正确性）：id 改 String 稳定身份 → `SecondaryFeatures:1719` 换 `resourceIdentifier` →
days 单位统一为周 → 里程碑改按 `level_id` → 站点勾选不再被裁剪写回 → 核实后端配置项 key 名。

第三批（安全）：`apiHosts` 收敛为硬编码 + 强制 https → 导航策略加 scheme/host 白名单 →
`logout()` 清 WebKit 数据 → 剪贴板加 `localOnly`+过期 → 脱敏黑名单全改 `contains` 并加 `rss` →
更新包加 SHA-256 校验 → 补 `PrivacyInfo.xcprivacy`。

第四批（性能与结构）：didSet 合并为脏标记 → WebSocket 加工挪进 detached →
TOML 解析结果提升为 `@State` → `ImageRenderer` 降 scale 并异步 → 拆 `SiteFeatures.swift`、
拆超长 body → 接线搜索筛选/排序/历史三件套。

---

## 七、必须在 Xcode 里验证的事

1. 是否真的编译通过（类型推断超时、跨文件同名类型冲突、拼错的 API 名，静态审查一概无法确认）。
2. 上表 #1/#5/#6 的崩溃是否实际触发 —— 取决于后端和网页返回什么，但这些是"外部输入直接终止进程"的路径，属于必须防的一类。
3. Release 的 `SWIFT_OPTIMIZATION_LEVEL` 实际生效值（pbxproj 确认未设置，默认值语义请在 Build Settings 里眼见为实）。
4. 运行时警告：`Publishing changes from within view updates`、`ForEach should only be used for constant data`、CoreGraphics NaN 告警。
5. `SharedViews.swift:1424-1449` 截图脱敏的 180ms `Task.sleep` 竞态 —— 列表长/主线程忙时可能截到未脱敏原文。
6. iPad 上跑一遍分享路径（`TARGETED_DEVICE_FAMILY` 含 iPad，`ActivityShareSheet` 未设 popover source）。
7. `SiteFeatures.swift:5696-5705` 的站点长截图截的是 WKWebView 原始页面，privacyMode 在这条路径上完全不生效，
   而 PT 个人页常直接显示 passkey/邮箱/IP/邀请码 —— 确认是否要脱敏。
